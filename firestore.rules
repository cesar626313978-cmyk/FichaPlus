rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permite lectura y escritura en todas las colecciones para sincronización en tiempo real
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
